# Spectre Base Theme Theme Directory

This directory contains the default WordPress theme files for Spectre Base Theme.

## Development notes

1. Keep theme metadata in style.css aligned with the repository identity.
2. Keep PHP functions and translation domains consistent with the default theme prefix.
3. Update templates in this directory as the WordPress theme surface evolves.
4. Run npm run dev in the project root during development.
5. Run npm run build before deploying.

## Deployment

Deploy the full spectre-theme directory to your WordPress installation:

    wp-content/themes/spectre-theme/

The dist directory is generated by Vite and contains the compiled theme assets.

Run `npm run package:theme` from the repository root (after `npm run build`)
to assemble a marketplace-ready `spectre-theme.zip` from this directory.

See the main README.md in the project root for full project documentation.

## License

This deployable WordPress theme package is distributed under
GPL-2.0-or-later -- see [LICENSE.txt](LICENSE.txt). It is built from the
`spectre-base` development repository, which is separately MIT-licensed (see
the repository root `LICENSE`). Compiled assets under `dist/` bundle
MIT-licensed Spectre packages; see [NOTICE.txt](NOTICE.txt) for their
preserved attribution.
